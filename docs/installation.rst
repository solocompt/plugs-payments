============
Installation
============

At the command line::

    $ easy_install plugs-payments

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv plugs-payments
    $ pip install plugs-payments
